# ⚡ QUICK FIX COMMANDS

Run these in order:

```bash
cd "D:\Claude\Projects\NSS Mumbai website\Website"

# 1. Complete cleanup
rm -rf .next out node_modules package-lock.json

# 2. Fresh install  
npm install

# 3. Test development
npm run dev
```

Open http://localhost:3000

## ✅ Should Now See:
- Styled navigation bar at top
- Dark background
- Proper layout
- Glass effect cards
- Everything looking professional

## ❌ If Still Broken:
Press F12 in browser → Console tab → Share any errors

---

## Once Dev Works:

```bash
# Build for GitHub
npm run build

# Test the build
npx serve out

# If looks good, push to GitHub
git add .
git commit -m "Fix: Complete styling overhaul"
git push
```

---

See `COMPLETE_STYLING_FIX.md` for detailed troubleshooting!
